__pycache__/
*.pyc

# Database files (large, rebuild locally)
data/*.db
data/*.db.bak
data/*.db-shm
data/*.db-wal
data/*.duckdb
data/*.duckdb.bak
data/*.duckdb.wal

# Raw data files (downloaded from GitHub, rebuild locally)
data/raw/

# Update metadata (auto-generated, machine-specific)
data/update_metadata.json

# Claude Code per-project settings (contains local permissions, can include secrets)
.claude/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Environment / secrets
.env
.env.*
!.env.example
