__pycache__/
*.pyc

# Database files (large, rebuild locally)
data/*.db
data/*.db.bak
data/*.db-shm
data/*.db-wal

# Legacy databases
legacy/*.db
legacy/*.db.bak

# Keep the pre-built legacy snapshot
!legacy/nflverse_custom.db.zip

# Update metadata (auto-generated)
data/update_metadata.json

# Environment
.env
